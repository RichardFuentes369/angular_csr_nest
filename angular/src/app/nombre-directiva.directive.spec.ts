import { NombreDirectivaDirective } from './nombre-directiva.directive';

describe('NombreDirectivaDirective', () => {
  it('should create an instance', () => {
    const directive = new NombreDirectivaDirective();
    expect(directive).toBeTruthy();
  });
});
